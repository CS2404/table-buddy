spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        default_batch_fetch_size: 100

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: DEBUG
        SQL: DEBUG